<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:labels="clr-namespace:DietApp.Views.Labels"
             x:Class="DietApp.Pages.ProfilePage">

    <NavigationPage.TitleView>
        <StackLayout Orientation="Horizontal">
            <Image WidthRequest="35" Aspect="AspectFit" HorizontalOptions="StartAndExpand" Source="vegetables"/>
            <labels:CustomLabel Text="{Binding FullName}" 
                                HorizontalOptions="StartAndExpand" 
                                FontSize="18" 
                                VerticalTextAlignment="Center"
                                Margin="-20, 0, 0, 0"/>
        </StackLayout>
    </NavigationPage.TitleView>
    
    <ContentPage.Content>
        <StackLayout>
            <Label Text="{Binding FullName}"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>