<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:labels="clr-namespace:DietApp.Views.Labels"
             xmlns:Views="clr-namespace:DietApp.Views"
             x:Class="DietApp.Pages.DietPage">

    <NavigationPage.TitleView>
        <StackLayout Orientation="Horizontal">
            <Image Source="vegetables" WidthRequest="35" Aspect="AspectFit" HorizontalOptions="StartAndExpand" />
            <labels:CustomLabel Text="{Binding CurrentDay, StringFormat='Ziua {0} din 15'}" 
                                HorizontalOptions="StartAndExpand" 
                                FontSize="18" 
                                VerticalTextAlignment="Center"
                                Margin="-20, 0, 0, 0"/>
        </StackLayout>
    </NavigationPage.TitleView>
    
        <ContentPage.Content>
        <StackLayout>

            <Views:DietCardView BindingContext="{Binding BreakfastCardViewModel}" />

            <Views:DietCardView BindingContext="{Binding LunchCardViewModel}" />

            <Views:DietCardView BindingContext="{Binding DinnerCardViewModel}" />
            
            
        </StackLayout>
    </ContentPage.Content>
</ContentPage>